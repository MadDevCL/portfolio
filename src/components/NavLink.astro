---
interface Props {
  href: string;
  title: string;
  large?: boolean;
}

const { href, title, large = false } = Astro.props;
---

<a
  href={href}
  class={`col-span-${large ? 6 : 4} md:col-span-2 font-montserrat font-semibold text-center text-xs md:text-sm md:py-1 md:px-4`}
  >{title}</a
>
